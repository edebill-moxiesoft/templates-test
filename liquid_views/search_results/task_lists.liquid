{% assign items_shown = 5 %}

<div class="section search_result unit task_list">

  <div class="header">
    <h3 class="task">{{ site.task_list_label | plural }}</h3>
  </div>

  {% include 'task_lists/search_results' limit: items_shown  hide_more: true hide_total: true %}

  {% if has_more_task_lists %}
  <div class="footer">
      <a class="more_link" href="/task_lists/search?q={{ query | escape }}&{{ extra_params }}">
      <span class="more_count">{% if num_hits > 1005 %}
      over 1000
      {% elsif num_hits > 5 %}
      {{ num_hits| minus:items_shown  }}
      {% endif %}</span>
      more</a>
  </div>
  {% endif %}

</div>
